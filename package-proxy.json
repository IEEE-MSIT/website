{
  "name": "image-proxy-server",
  "version": "1.0.0",
  "description": "Express.js proxy server to bypass CORS issues for Google Drive images",
  "main": "image-proxy-server.js",
  "scripts": {
    "start": "node image-proxy-server.js",
    "dev": "nodemon image-proxy-server.js",
    "test": "echo \"No tests specified\" && exit 1"
  },
  "keywords": [
    "cors",
    "proxy",
    "images",
    "google-drive",
    "express"
  ],
  "author": "IEEE MSIT Web Team",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
